spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true

jwt:
  issuer: j ... r
  secret_key: s ... t

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1 ... .com
            client-secret: G ... d
            scope:
              -email
              -profile
