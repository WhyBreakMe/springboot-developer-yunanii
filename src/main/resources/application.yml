spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: true

jwt:
  issuer: jines8115@snu.ac.kr
  secret_key: study-springboot

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1 ... .com
            client-secret: G ... d
            scope:
              -email
              -profile
